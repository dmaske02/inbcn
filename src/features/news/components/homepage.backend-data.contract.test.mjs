import assert from "node:assert/strict";
import { readFile } from "node:fs/promises";
import test from "node:test";

async function sources(){return [(await readFile(new URL("./homepage.tsx",import.meta.url),"utf8")),(await readFile(new URL("./homepage-sections.tsx",import.meta.url),"utf8"))];}
test("homepage renders only snapshot collections through reusable sections",async()=>{const [homepage,sections]=await sources();assert.doesNotMatch(homepage+sections,/const stories\s*=|const rails\s*=|Monsoon session opens|Cabinet clears|climate-ready neighbourhoods/u);for(const field of ["featured","topHeadlines","latest","trending","categoryRails","editorPicks"])assert.match(homepage,new RegExp(`data\\.${field}`,"u"));for(const component of ["HomepageHeroSection","HomepageHeadlineSection","HomepageFeedSection","HomepageRankedSection","HomepageCategoryRails","HomepageEditorsSection"])assert.match(homepage,new RegExp(component,"u"));});
test("shared story images preserve hero priority and resolved image behavior",async()=>{const [,source]=await sources();assert.match(source,/HomepageStoryImage/u);assert.match(source,/loading=\{priority\?"eager":"lazy"\}/u);assert.match(source,/fetchPriority=\{priority\?"high":"auto"\}/u);assert.match(source,/getHeroImagePresentation\(story\.image\)/u);assert.match(source,/maxWidth:presentation\.maxWidth/u);assert.match(source,/<h1><Link href=\{story\.href\}>\{story\.title\}<\/Link><\/h1>/u);});
test("desktop homepage image and typography safeguards remain unchanged",async()=>{const styles=await readFile(new URL("../../../app/globals.css",import.meta.url),"utf8");assert.match(styles,/@media\(min-width:761px\)\{\.proto-hero-photo\{height:100%;aspect-ratio:auto\}\}/u);assert.match(styles,/\.proto-hero-photo img\{object-position:center\}/u);assert.match(styles,/\.proto-hero-copy h1\{[^}]*-webkit-line-clamp:6[^}]*\}/u);assert.match(styles,/\.proto-brief p\{[^}]*-webkit-line-clamp:3[^}]*\}/u);assert.match(styles,/@media\(min-width:1041px\)\{\.proto-hero-copy h1\{width:calc\(100% \+ 16px\)/u);});
